{
  "name": "cqf-lexicon",
  "version": "0.1.0",
  "private": true,
  "description": "CQF vocabulary and study plan web app for Feishu.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "generate:study-plan": "node scripts/generate-study-plan.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
